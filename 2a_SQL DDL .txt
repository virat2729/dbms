create database practicalsdb;
  use practicalsdb;
  show tables;
  create table t1(cname varchar(30), roll_no int primary key);
  desc t1;
  alter table t1 add gender varchar(6);
  alter table t1 add address int;
  desc t1;
  alter table t1 add mno int;
  desc t1;
  alter table t1 drop mno;
  alter table t1 change address addr varchar(50);
  alter table t1 rename candidate;
  desc candidate;
  insert into candidate values('Lilly', '01', 'Female', 'Pune');
  insert into candidate values('Gaurav', '02', 'male', 'Pune');
  insert into candidate values('Shreyas', '03', 'male', 'Buldhana');
  insert into candidate values('sham',4,'male','shaha');
  insert into candidate values('Pranav',5,'male','kolhapur');
  insert into candidate values('Varun',6,'male','bhandara');
  insert into candidate values('Prachi',7,'female','natepute');
  insert into candidate values('meghna',8,'female','nagpur');
  insert into candidate values('prose',9,'female','sambhajinagar');
  select * from candidate;
  create view v1 as select cname, roll_no, addr from candidate;
  desc v1;
  select * from v1;
  create view v2 as select cname , roll_no, gender from candidate;
  desc v2;
  select * from v2;
  create index idx1 on candidate(cname);
  show index from candidate;
  alter table candidate drop roll_no;
 select * from candidate;
  alter table candidate add roll_no int primary key auto_increment;
  select * from candidate;
